# 日麻牌效训练器开发计划

## 1. 项目目标

本项目是一款面向手机浏览器的日麻何切训练器，目标是让初学者按照明确的牌效原则循序渐进地练习，最终过渡到包含打点、牌河、顺位和攻防判断的复杂局面。

产品以纯静态网页发布到 GitHub Pages，并支持安装为 PWA。正式运行时不依赖服务器、数据库或在线 AI，也不在浏览器中保存任何 API Key。

核心原则：

1. 麻将规则、向听、受入、分割和答案必须由确定性程序产生。
2. AI 可以协助提取规则、提出题目模板和润色解释，但不能担任答案裁判。
3. 正式题目必须在构建阶段完成生成、验证和解释，网页只读取已经验证的静态数据。
4. 运行时交互必须适合单手操作，不要求拖动细小元素或打开复杂菜单。
5. 所有题目都必须能够追溯到明确的牌效原则、生成模板和验证结果。

## 2. 已确定的技术与素材

- 前端：React、TypeScript、Vite。
- 发布：GitHub Actions + GitHub Pages。
- 离线能力：PWA Service Worker。
- 牌面：[FluffyStuff/riichi-mahjong-tiles](https://github.com/FluffyStuff/riichi-mahjong-tiles)，CC0。
- 基础麻将逻辑：优先评估 `@kobalab/majiang-core`，MIT。
- 构建期独立校验：使用第二套向听实现交叉验证，例如 `MahjongRepository/mahjong`。
- 4.0 局面评价：Mortal，仅作为离线题目生成和筛选工具，不在网页中实时调用。
- 参考产品：Euophrys/Riichi-Trainer。除非项目整体接受 GPL-3.0，否则只参考交互和行为，不复制其 GPL 代码。

## 3. 建议的仓库结构

```text
apps/
  web/                     # 手机网页和 PWA

packages/
  domain/                  # 牌、手牌、场况和题目的类型定义
  solver/                  # 向听、受入、打牌比较
  decomposition/           # 分割规则表、锁定和循环状态机
  curriculum/              # 原则登记、前置关系和课程进度
  generator/               # 参数化题目生成器
  validator/               # 合法性、答案和难度验证
  explanation/             # 结构化证明和模板解释
  mortal-adapter/          # 4.0 离线分析适配器

content/
  principles/              # 人工审核的牌效原则
  templates/               # 题目生成模板
  seeds/                   # 人工种子题
  generated/               # 构建生成的正式静态题目
  explanations/            # 人工审核的解释模板

scripts/
  build-shape-rules/       # 构建运行时分割规则表
  generate-questions/      # 批量生成题目
  validate-content/        # 全量验证
  build-coverage-report/   # 生成教材与原则覆盖报告
  analyze-with-mortal/     # 4.0 离线局面分析

tests/
  golden/                  # 已人工确认的标准牌姿
  differential/            # 两套求解器交叉验证
  metamorphic/             # 花色互换、镜像等变形测试
  regression/              # 曾经出现过的错误题目
```

## 4. 统一牌表示

内部统一采用 34 种牌索引，同时保留人类可读字符串：

```text
1m-9m  万子
1p-9p  饼子
1s-9s  条子
1z-4z  东南西北
5z-7z  白发中
0m/0p/0s 赤五
```

任何显示顺序都不能改变内部牌 ID。分组、答案和统计只能使用 ID，不能依赖牌在屏幕上的位置。

## 5. 自动理牌和显示顺序

### 5.1 顶层顺序

每手牌从左到右始终为：

1. 三门数牌；
2. 风牌；
3. 箭牌。

### 5.2 数牌花色顺序

万、饼、条的显示顺序在每道题生成时随机决定，只允许六种排列：

```text
万饼条  万条饼
饼万条  饼条万
条万饼  条饼万
```

生成结果必须写入题目数据的 `suitOrder`，答题过程中保持不变。重载同一道题也必须保持相同顺序，不能在前端重新随机。

这一随机化只影响显示，不影响牌姿、答案和验证。

### 5.3 风牌顺序

风牌始终按照以下顺序显示：

```text
东 → 南 → 西 → 北
```

不允许反转或任意洗牌。

### 5.4 箭牌顺序

箭牌只允许保持宝牌循环的循环旋转：

```text
白发中
发中白
中白发
```

例如 `中发白`、`白中发` 均不允许。1.0 固定使用 `白发中`；2.0 起由题目生成器在三种合法循环中随机选择，并写入 `dragonOrder`。

该约束用于强化以下宝牌记忆：

```text
白 → 发 → 中 → 白
```

## 6. 手牌分割辅助系统

### 6.1 目标

分割功能不是装饰，而是课程核心。玩家应能看到并操作手牌中的：

- 完成面子；
- 雀头或对子；
- 两面、嵌张、边张；
- 复合形；
- 浮牌。

系统需要保存一个牌姿的所有合理分割方式，而不是只保存单一答案。

### 6.2 合理分割定义

1.0 中，“合理分割”指不考虑场况、牌河、宝牌和打点时，能够参与理论最优向听或理论受入分析，并且没有被其他分割完全支配的分割。

“完全支配”至少包括：

- 向听数不更差；
- 面子与有效搭子结构不更差；
- 理论进张种类或枚数更多；
- 没有损失该分割独有的有效变化。

特殊手路线必须显式控制。普通手课程可以在题目模板中关闭七对子、国士或染手分割，避免出现课程外答案。

### 6.3 规则表驱动，不在点击时计算

玩家点击牌时，不得即时运行向听搜索或受入枚举。

实现方式：

1. 构建阶段离线枚举数牌局部形状。
2. 为每种形状生成稳定的 `shapeKey`，例如一门数牌的九位计数向量。
3. 离线计算所有合理分割及其理论进张。
4. 按“不考虑场况的理论进张数从多到少”排序。
5. 理论进张完全相同时，依次比较好型绝对枚数、二阶改良宽度、结构打点潜力和分割简洁度。
6. 所有指标仍完全相同时，使用规范化分割 ID 排序，禁止依赖对象遍历顺序。
7. 将结果编译为版本化规则表，随网页发布。
8. 运行时点击只进行规则表查询和索引递增。

完整排序键如下：

```text
1. 理论进张总枚数，降序
2. 好型绝对枚数，降序
3. 二阶改良宽度，降序
4. 不依赖场况的结构打点潜力，降序
5. 分割简洁度，降序
6. 规范化分割 ID，升序
```

其中：

- 好型严格指两面搭子：`23` 至 `78` 这种两张相邻数牌同时等待左右两种进张，例如 `45` 等 `3/6`。对子、嵌张、边张以及仅仅拥有多种受入的形状都不算好型。每张进张必须按它与两面的直接关系分类：摸到既有两面的任一侧（如 `23p` 摸 `1p/4p`）属于好型进张；同向听下新增一组两面（如孤张 `3m` 摸 `2m` 形成 `23m`）属于两面改良。评价使用这些好型进张与两面改良的剩余绝对枚数，不用百分比；字牌对子进张不计入好型枚数。不能因为手牌其他位置原本已有两面，就把字牌成对等普通进张染成好型。即使七对子当前向听更低，数牌形成两面而保留转回普通手路线的机会仍须计入结构改良。
- 改良列表不限于两面：任何同向听摸牌只要在最佳再切后增加总受入、降低普通手向听、增加完成面子、增加普通手受入、新增局部连接或新增两面，就属于需要重新考虑的结构进张。局部连接必须逐门独立判断，不能因为其他花色拥有更高的全手受入基线而被掩盖；例如万子同为 `1366m` 时，无论切的是 `8p` 还是 `8s`，摸 `8m` 形成 `68m + 6m` 的分割机会都必须列为改良。`799m` 摸 `8m` 形成 `789m + 9m`，即使总向听与受入暂时不变，也同样必须列入；其中只有新增两面的改良计入好型绝对枚数。本巡刚切出的牌种不作为本次切牌带来的改良，避免以“摸回刚切的牌”反向奖励该选择；其余同种牌仍按正常牌山枚数参与真实受入与次巡期望。
- 二阶改良宽度表示下一次进张之后仍然保留的改良种类和枚数。
- 结构打点潜力只评价不依赖当前场况的结构，例如平和、断幺、三色和一气通贯的保留可能性。
- 当前宝牌、场风、自风、牌河和顺位不参与分割循环排序。它们可以改变题目标准答案，但不能让相同局部形状的点击循环顺序发生变化。
- 这些指标全部在构建规则表时计算，玩家点击时不计算。

规则表记录示意：

```json
{
  "shapeKey": "0,1,2,1,0,0,0,0,0",
  "cycle": [
    {
      "groups": ["23", "34"],
      "theoreticalUkeire": 12
    },
    {
      "groups": ["234", "3"],
      "theoreticalUkeire": 8
    }
  ],
  "forced": false,
  "ruleVersion": "1"
}
```

注意：具体数值必须由正式算法生成，上例只展示数据结构。

### 6.4 点击交互

首次发牌时，手牌正常理牌，但不要求玩家逐组选择。

玩家第一次点击手牌中的任意牌时：

1. 1.0 使用题目携带的教材形状规则；2.0 对每门牌使用摸牌时已经生成的完整分割快照；
2. 所有合理分割共同拥有的非单张组自动锁定；单张始终是浮牌，永不锁定；
3. 如果被点击的牌所属花色存在多解，则该花色进入循环的第一种完整分割；
4. 如果被点击的牌属于已锁定形状，则不改变锁定状态，也不产生无效动画。

对于多种分割可能，点击该花色任意一张牌都循环整门牌的完整分割，而不是只操作被点击的局部。2.0 的完整分割必须覆盖杠、刻子、顺子、对子、两张搭子、连续嵌张复合搭子和浮牌；例如 `2222446s` 至少保留 `[222][24][46]`、`[2222][44][6]`、`[2222][46][4]` 与 `[222][246][4]`。

例如 `2334`：

```text
未标记
  点击任意一张
[23][34]
  再点击任意一张
[234][3]
  再点击任意一张
[23][34]
```

循环顺序永远使用规则表中已预先排好的顺序，即不考虑场况的理论进张数从多到少。运行时不能因为场风、自风、宝牌、牌河或玩家选择改变循环顺序。

### 6.5 自动锁定

如果一个非单张组存在于该花色的所有合理完整分割中，则在首次手牌交互时自动锁定。四张相同牌必须同时考虑杠与刻子加浮牌等路线，不能直接当作普通刻子锁定。

锁定后的表现：

- 使用固定括号或间隔表示分组；
- 显示低干扰的锁图标；
- 点击其中任意牌不循环；
- 玩家开始自由拖动时退出当前自动分组显示，避免旧括号和新手牌顺序不一致；
- 锁定只代表当前牌姿下分割唯一，不代表这些牌直到本局结束永远固定。

### 6.6 进张后的自动解锁

任何摸牌、吃碰杠或手牌数量变化都是一次 `handChanged` 事件。只有在该事件发生时重新查询规则表；普通点击不重新计算。

处理流程：

1. 记录旧局部形状和旧分割 ID。
2. 应用摸牌或牌姿变化。
3. 对受影响花色生成新的 `shapeKey`。
4. 查询预生成规则表。
5. 如果旧锁定分组在新形状中不再是所有合理分割的公共部分，清除其锁定，但暂不显示新的分割。
6. 如果新形状只有一种合理分割，将其标记为“等待自动锁定”，不立即显示锁定结果。
7. 如果旧的手动分割在新规则中仍然存在，则尽量保留；否则回到新形状循环列表的第一项。
8. 玩家下一次点击手牌中的任意牌时，统一显示所有等待中的解锁、重新分割和自动锁定结果。

示例：

```text
原手牌：[234]🔒
摸入：3
新形状：2334
结果：原 [234] 自动解锁，形状变为可循环的 2334
```

首次发牌和 2.0 以后的连续进张使用相同原则：系统可以在 `handChanged` 时查询规则并准备新状态，但必须等待玩家下一次点击手牌后才显示自动锁定或新分割。动画必须简短且可关闭。

### 6.7 分割视觉与无障碍

- 不能只依靠颜色区分分组。
- 必须同时使用间距、括号、下划线或外框。
- 触控区域至少覆盖整张牌，不要求点击牌缝。
- 不使用双击、长按作为必需操作。
- 点击切换分割时，上方手牌按当前分组顺序同步排列；下方说明必须与上方逐组对应并带 `m/p/s` 后缀。
- 支持关闭动画和减少动态效果。
- 屏幕阅读器标签应说明牌名、分组类型、是否锁定和当前循环序号。

## 7. 原则知识库与覆盖保证

### 7.1 原则登记格式

每条原则必须包含：

```yaml
id: TE-EXCESS-002
name: 搭子过剩时淘汰弱搭
stage: 1.0
prerequisites:
  - TE-BLOCK-001
statement: "..."
conditions:
  - 普通手
exceptions:
  - 七对子路线
  - 染手路线
sources:
  - work: 魔女麻将
    location: 待录入章节或页码
questionFamilies:
  - excess_tatsu
positiveExamples: []
counterExamples: []
```

### 7.2 提取流程

1. 录入教材完整目录、章节和页码范围。
2. 逐节提取显式原则、隐含条件、正例和反例。
3. 每一段教材内容必须映射到原则、术语、例题、反例或“无需进入产品”的明确记录。
4. 合并重复原则时保留所有来源位置。
5. 每条原则至少有一个正例、一个边界例和一个反例。
6. 每条进入课程的原则必须关联生成模板和自动测试。
7. 生成覆盖报告，禁止存在无来源原则或无测试原则。

“无疏漏”的工程定义是：对登记的教材版本，所有章节均已审阅并在覆盖报告中有去向；不声称覆盖麻将中一切可能的战略知识。

### 7.3 版权边界

- 可以提取和重新组织牌效知识。
- 原则说明使用自己的表达。
- 不批量复制教材原文、插图和原题。
- 题目使用规则生成器产生原创牌姿。
- 来源字段用于内部追踪，不在未获授权时展示大段原文。

## 8. 题目生成

### 8.1 构造式生成

禁止仅通过随机发十四张牌生成正式课程题。正式题目由结构模板构造，例如：

```yaml
id: TEMPLATE-EXCESS-TATSU-001
principleId: TE-EXCESS-002
stage: 1.0
required:
  completedMelds: 2
  pairs: 1
  strongTatsu: 1
  weakTatsu: 2
constraints:
  shantenBefore: [1, 2]
  uniqueBest: true
  plausibleDiscardsMax: 3
  minimumUkeireGap: 6
```

允许的变换包括：

- 万、饼、条置换；
- `1↔9、2↔8` 数牌镜像；
- 不越界时整体平移；
- 替换与目标原则无关的完成面；
- 替换雀头；
- 调整场风、自风和宝牌；
- 调整显示用 `suitOrder` 与合法的 `dragonOrder`。

### 8.2 题目验证

所有正式题目必须验证：

- 手牌、牌山、赤牌和场况合法；
- 任意牌最多四张；
- 向听数符合当前版本；
- 每一种可切牌都已枚举；
- 切牌后的向听、受入种类和剩余枚数正确；
- 七对子、国士、染手等课程外路线没有产生意外答案；
- 目标原则确实决定答案；
- 分割规则表覆盖该牌姿；
- 题目解释能由结构化证明完整生成；
- 相同内容不会因显示花色顺序产生重复题 ID。

### 8.3 1.0 筛选标准

- 发牌时为一向听或二向听。
- 玩家只切一次牌。
- 最多三个合理切牌候选，但所有手牌仍然可以点击。
- 最优答案原则上唯一；明确设计的等价答案必须全部接受。
- 最优与第二名差距明显。最终阈值在可玩原型完成后根据体感校准；生成器在此之前必须输出绝对差、相对差和候选数量，阈值只能通过版本化配置调整。
- 一道题主要训练一个原则，最多附带一个已学前置原则。
- 唯一场况信息为场风、自风和宝牌。
- 1.0 启用赤五。
- 1.0 仍以牌效为主，但宝牌允许改变标准答案。此类题必须显式标记为宝牌影响题，并保证综合评价下最佳与次优仍有明显差距；普通牌效题不能因意外宝牌配置悄悄改变目标答案。

“合理切牌候选”的初始定义：不增加最小向听数，并且受入达到最佳选择的指定比例，或者能参与至少一种非劣分割。该定义必须版本化，不能在题库生成后静默改变。

## 9. 答案、证明和解释

每道题必须保存结构化证明，而不是只保存一个答案字符串：

```json
{
  "bestDiscards": ["3m"],
  "principleIds": ["TE-EXCESS-002"],
  "beforePartitions": [],
  "discardResults": {
    "3m": {
      "shanten": 1,
      "ukeireTiles": ["2m", "5m"],
      "ukeireCount": 8,
      "recommendedPartitionId": "partition-a"
    }
  },
  "comparisonReason": "same-shanten-more-ukeire",
  "solverVersion": "...",
  "shapeRuleVersion": "..."
}
```

解释由模板生成，至少包含：

1. 正确切牌；
2. 切牌前推荐分割；
3. 切牌后推荐分割；
4. 向听数；
5. 有效牌种类和剩余枚数；
6. 与主要错误选择的对比；
7. 本题所训练的原则；
8. 玩家分割与推荐分割的差异。

AI 只为有限数量的“解释族”编写和润色模板，例如“同向听比较受入”“破坏两面”“固定雀头错误”。模板经人工审核后固定下来。十万道题不进行十万次 AI 调用。

## 10. AI 开发工具接口

开发期可以向 AI 暴露确定性工具：

```text
analyzeHand(hand, context)
getShapeRule(shapeKey)
enumeratePartitions(hand, profile)
compareDiscards(hand, context, profile)
generateVariants(seed, constraints)
validateQuestion(question)
renderExplanation(proof, templateId)
```

AI 提出题型和约束，生成器产生候选，验证器决定是否合格。AI 不得覆盖验证器结论，也不得直接把未经验证的牌姿写入正式题库。

## 11. 难度模型

题目保存多维难度，不只保存简单、普通、困难：

```yaml
difficulty:
  shanten: 2
  plausibleDiscards: 3
  partitionAmbiguity: 2
  ukeireGap: 8
  lookaheadDepth: 1
  specialRoutes: 0
  valueConflict: 0
  situationalPressure: 0
  defenseRequired: 0
```

初期由规则计算难度；积累答题数据后，可以加入正确率、平均用时、首次正确率和错误分布进行校准，但不能自动改变原则顺序。

## 12. 版本路线

### 12.1 版本 1.0：单次基础牌效

- 每题只有一手一向听或二向听的牌。
- 玩家只进行一次切牌选择。
- 合理候选不超过三个。
- 最优与次优差距明显。
- 支持分割循环、自动锁定和答案后分割解释。
- 场况只有场风、自风和宝牌。
- 启用赤五。
- 箭牌固定按白、发、中显示，不进行循环旋转。
- 宝牌可以改变答案，但宝牌影响题仍必须具有明显答案差距。
- 完成章节、错题本、掌握度和进度保存。

建议课程顺序：

1. 面子、对子、搭子和浮牌；
2. 两面、嵌张和边张；
3. 向听与受入；
4. 孤立牌比较；
5. 搭子比较；
6. 复合形分割；
7. 五搭子与搭子过剩；
8. 无雀头与固定雀头；
9. 一向听类型；
10. 宝牌、场风和自风；
11. 综合练习。

### 12.2 版本 2.0：连续牌效

- 一手牌连续摸切直到听牌或题目结束。
- 初始可为四向听、五向听等散牌。
- 允许普通进张和改良进张。
- 从 2.0 开始引入七对子、国士无双与普通手路线的比较。
- 合理切牌数量和答案差距不设 1.0 限制。
- 每次 `handChanged` 后准备新的分割规则状态，并等待玩家下一次点击后显示解锁或重新锁定。
- 从 2.0 开始，箭牌在白发中、发中白、中白发三种合法循环中随机显示。
- 每次开始连续训练时在浏览器内即时洗出完整合法牌山；起手、宝牌指示物、后续进张、数牌顺序与箭牌循环全部随机。当前局保存随机种子，可用于复现问题牌局。
- 不调用在线 AI。
- 未来原定 3.0 的好型枚数、二阶改良、价值路线与特殊手继续加入这一套连续训练，不另做割裂的训练入口；“3.0”只表示评价器与题目内容升级。

### 12.3 版本 3.0：价值路线与特殊手

- 清一色、混一色；
- 三色、一气通贯、断幺等役种路线；
- 宝牌与速度冲突；
- 两步以上手变；
- 必要时加入鸣牌可能性。

3.0 开始必须明确题目的评价目标，不能默认所有题都只按当前受入排序。

3.0 同时保存并向玩家解释三个维度：

1. **速度**：当前向听、当前受入、固定摸牌窗口内的听牌率和和了率。
2. **条件打点**：在最终和了的样本中，平均得点、役种成立率和平均番数。
3. **进攻期望值**：在不考虑对手攻击和放铳的前提下，对各和了结果进行 `概率 × 得点` 汇总。

常用评价方式及本项目用途：

| 评价方式 | 优点 | 缺点 | 用途 |
| --- | --- | --- | --- |
| 向听数与当前受入 | 最透明，适合教学 | 看不到手变和打点 | 1.0 主标准 |
| N 摸听牌率/和了率 | 能体现速度和二阶改良 | 需要固定模拟窗口 | 2.0、3.0 速度指标 |
| 条件平均打点 | 容易解释役种和宝牌收益 | 不包含做成这手牌的概率 | 3.0 打点指标 |
| 进攻期望值 | 可以统一比较速度和价值 | 依赖模拟假设 | 3.0 综合题默认标准 |
| 完整局收支/顺位期望 | 能包含攻防与点棒状况 | 难解释且依赖模型 | 4.0 |

3.0 的默认方案：

- 使用无对手攻击的离线摸牌模拟或等价动态规划，同时计算多个固定窗口，例如未来 6 摸和 12 摸。
- 纯速度课仍以速度指标为答案，不被打点覆盖。
- 役种与价值课可以条件打点为主，但必须同时展示速度损失。
- 综合题使用进攻期望值筛选，并同时展示速度与打点两条解释，不能只显示一个神秘总分。
- 最佳与次优落在模拟误差或配置容差内时接受多个答案，或把题目标记为讨论题。
- 所有窗口、权重、模拟次数和随机种子均写入版本化评价配置，试玩后可以调整，但已发布题目不静默重算。

### 12.4 版本 4.0：场况与攻防

增加：

- 局数、巡目、本场和立直棒；
- 四家点棒与顺位；
- 四家牌河及手出、摸切历史；
- 副露和立直状态；
- 可见牌、安全牌、筋、壁等防守信息；
- 和了率、放铳率、局收支和顺位期望。

4.0 使用 Mortal 离线分析预生成题目，不在手机网页中运行 Mortal。处理流程：

1. 从合法模拟局面或获得授权的牌谱中提取候选决策点。
2. 转换为固定版本的 MJAI 事件序列。
3. 使用固定 Mortal 模型和配置分析全部合法动作。
4. 保存动作评分、模型版本和分析参数。
5. 使用规则模型提取速度、打点和安全度特征。
6. 优先保留 Mortal 最终推荐动作稳定、且规则侧最佳与第二名差距明显的局面。
7. 不把 Mortal 的候选动作 Q 值差单独当作题目可信度或教学理由；Mortal 的目标是选择行动，不是精确评价所有备选动作。
8. 条件允许时使用第二评价器、相邻稳定模型版本或局面扰动进行交叉验证。
9. 模型意见不一致的局面作为讨论题，不设置唯一答案。
10. 静态导出题目和解释后再发布。

每道 4.0 题必须声明优化目标，例如局收支、半庄顺位期望、保一或避四，不能笼统称为绝对最佳何切。

4.0 的默认内容策略是面向初学者筛选高差距、可用规则解释的普通场况，不优先选择模型之间容易产生完全不同判断的边界题。开始制作 4.0 内容时选择当时较新的稳定 Mortal 模型，固定权重文件哈希、代码提交和配置；之后升级模型时批量复核题库，不使用不固定版本的“latest”。特殊顺位题仍需单独声明其优化目标。

## 13. 实施顺序

### Milestone 0：项目骨架

- 创建 monorepo、React/Vite 应用和测试环境。
- 接入 CC0 牌图。
- 定义牌、场况、分割、题目和证明的 TypeScript 类型。
- 建立 GitHub Actions 基础验证。

完成标准：手机浏览器能打开一手静态牌，牌面无变形，测试和构建可重复执行。

### Milestone 1：求解器可信基础

- 接入或实现向听计算。
- 实现受入枚举和剩余枚数计算。
- 枚举所有切牌结果。
- 接入第二求解器进行差分测试。
- 建立标准牌姿和变形测试。

完成标准：标准题、花色置换和镜像题结果一致；两套求解器没有未解释差异。

### Milestone 2：分割规则编译器

- 定义局部形状 `shapeKey`。
- 离线枚举合理分割。
- 计算并固定理论进张排序。
- 生成版本化规则表。
- 为 `2334` 等核心复合形建立黄金测试。

完成标准：点击交互不运行向听搜索；相同规则版本在任何设备上产生完全相同的循环顺序。

### Milestone 3：分割手机原型

- 实现首次点击自动锁定。
- 实现点击形状任意牌循环整个分割。
- 实现进张后的事件式解锁。
- 实现随机数牌花色顺序和合法字牌顺序。
- 实现分组无障碍标签和减少动画选项。

完成标准：至少覆盖完成面、简单搭子、`2334`、更长复合形以及锁定后进张解锁场景。

### Milestone 4：原则登记与题目流水线

- 录入教材目录。
- 建立原则登记表和依赖关系。
- 为第一批原则编写种子题和生成模板。
- 完成候选生成、验证、去重和覆盖报告。

完成标准：任意正式题目都能追溯到原则、模板、生成参数、求解器版本和验证记录。

### Milestone 5：1.0 产品

- 完成单次何切界面。
- 完成答案后分割与受入解释。
- 完成课程、进度、错题本和复习。
- 完成 PWA、离线缓存和 GitHub Pages 发布。
- 使用真实手机进行触控和布局测试。

完成标准：满足第 12.1 节全部要求，并通过全量题库验证。

### Milestone 6：2.0 连续训练

- 加入确定性牌山和摸切状态机。
- 每次进张触发分割更新。
- 同向听候选同时评价当前受入、好型绝对枚数与改良机会；展开比较时可查看各候选及好型/改良/非好型明细。
- 支持暗杠、岭上牌、追加宝牌指示物和相应可见牌扣减。
- 显示进张与改良历史。
- 支持完整一手的复盘。

### Milestone 7：3.0 价值路线

- 作为连续训练的内容与评价升级，不建立另一套平行玩法。
- 扩展评价器和题目目标。
- 加入特殊手、役种与打点路线。
- 建立多答案和路线型解释。

### Milestone 8：4.0 攻防

- 定义完整局面 Schema 和 MJAI 转换。
- 建立 Mortal 独立离线流水线。
- 固定模型、参数和可复现环境。
- 建立多评价器筛题和讨论题机制。
- 发布预生成的复杂何切题。

## 14. 测试要求

### 14.1 单元测试

- 牌编码和排序；
- 花色六种排列；
- 风牌固定顺序；
- 箭牌只允许三种循环旋转；
- `shapeKey` 稳定性；
- 分割循环和回绕；
- 强制锁定；
- 进张解锁；
- 题目 Schema；
- 解释模板缺失字段检测。

### 14.2 黄金测试

至少包含：

- 唯一完成面；
- 唯一对子；
- `2334` 两种分割；
- 完成面进张后变为复合形；
- 多个互相重叠的复合形；
- 同向听不同受入；
- 宝牌打破接近选择；
- 风牌与箭牌宝牌循环。

### 14.3 变形测试

- 万饼条置换后答案随牌 ID 等价变化；
- 数牌镜像后分割和答案等价；
- 替换无关完成面后核心答案不变；
- 改变 `suitOrder` 后答案完全不变；
- 相同题目重载后显示顺序、分割循环和解释不变。

### 14.4 内容发布门禁

以下任一情况必须阻止题库发布：

- 两套求解器结果不一致；
- 正式题没有来源原则；
- 原则没有测试或生成模板；
- 最佳答案不符合版本筛选标准；
- 存在未声明并列答案；
- 分割规则缺失；
- 解释无法完整渲染；
- 题目或 Mortal 分析缺少版本信息。

## 15. 数据版本与可复现性

每次内容构建记录：

- Git commit；
- 题库 Schema 版本；
- 原则库版本；
- 求解器版本；
- 分割规则表版本；
- 生成器版本；
- 随机种子；
- 解释模板版本；
- 4.0 使用的 Mortal 代码、模型和配置版本。

已经发布且被玩家做过的题目，其题目 ID 和内容不可静默修改。答案算法改变时生成新版本；旧题保留迁移记录。

## 16. 当前明确不做的事项

- 1.0 不加入完整牌河、点棒和攻防判断。
- 1.0 不实时生成题目。
- 正式网页不调用在线大模型。
- 正式网页不运行 Mortal。
- 不复制教材原文和原题建立题库。
- 不把显示排序作为麻将算法输入。
- 不在每次点击分割时重新计算向听或受入。
- 不把 AI 单模型输出称为无条件正确答案。

## 17. 已确认的产品决策与待校准参数

### 17.1 已确认

1. 七对子和国士无双从 2.0 开始引入。
2. 1.0 启用赤五。
3. 1.0 以牌效为主，但宝牌可以改变答案；宝牌影响题不能出现过于接近的主要选项。
4. 连续摸牌后先准备分割状态，等待玩家下一次点击手牌才显示自动解锁或锁定。
5. 分割同受入时依次比较好型绝对枚数、二阶改良、结构打点、分割简洁度和规范 ID。
6. 1.0 箭牌固定为白发中；2.0 起在三个合法循环中随机。
7. 3.0 同时计算速度、条件打点和进攻期望值；不同课程使用不同主指标，综合题默认使用透明的进攻期望值。
8. 4.0 使用制作内容时较新的稳定 Mortal 模型，固定精确版本；以高差距、适合初学者且可解释的题目为主。

### 17.2 待原型试玩后校准

1. 1.0 “合理候选”的筛选阈值。
2. 1.0 最优与次优所需的最小绝对差和相对差。
3. 宝牌足以改变答案时允许承受的最大速度损失，以及所需的最小综合差距。
4. 3.0 模拟窗口、模拟次数、容差和可能使用的权重。

这些参数必须集中写入版本化配置并保留题库构建记录，不能散落在 UI 代码中，也不能在题目发布后静默修改。

## 18. 1.0 实施状态（2026-08-19）

当前代码已形成可发布候选版：

- React/Vite 手机网页、PWA 清单、离线缓存和 GitHub Pages 工作流已完成。
- 使用 FluffyStuff/riichi-mahjong-tiles 的 CC0 浅色牌面，授权副本随项目保留。
- 标准形向听、受入、剩余枚数、宝牌和赤五区分已实现。
- 完成面首次点击锁定、复合形规则表循环和“手牌变化后等待下一次点击再显示”的状态机已实现并有黄金测试。`246` 等连续嵌张复合搭子在专用规则和答案推荐分割中均作为整体显示，不拆成单张加嵌张。
- 14 个教材原则各有人工原创种子题；第二版离线生成器组合花色置换、数字镜像、风牌身份轮换、箭牌轮换和课程专用场景模板，发布 84 道静态题。第一课明确覆盖客风与役牌、只有风牌、只有箭牌、单张客风、多张风牌和字牌对子。每题保留模板 ID、生成器版本、变换与场景参数。
- 全量题库校验覆盖 14 张合法性、1/2 向听、答案有效性、合理候选不超过 3 个、赤五、六种花色顺序、固定白发中顺序和分组不重叠。
- 答题页默认处于切牌模式；整理手牌与分组作为独立辅助工作区。玩家可拖动重排，并能在整理中及答题后恢复题目原始自动理牌顺序。
- 推荐分割绑定实际牌张、推荐切牌和玩家当前顺序动态生成，保留花色、赤五 `0` 和日文汉字字牌表示。候选比较无论答对与否都会显示推荐项及一至两个主要错误选项。
- 课程、答题反馈、受入比较、进度、错题复习和本机持久化已完成；手机尺寸的主流程和关键分割交互已经过浏览器验收。
- 当前自动测试共 21 项，覆盖牌表示、分割状态、推荐分割、向听、受入和题库发布约束；生产构建通过。

当前合理候选暂定为同向听且受入不低于最优的 91%，仅作为可试玩原型参数。正式发布前仍需在真实手机上试玩，并由产品方按体感校准第 17.2 节参数。GitHub Pages 工作流已经就绪，但实际发布需先建立/指定 GitHub 仓库，并在仓库 Pages 设置中选择 GitHub Actions。

## 19. 后续资料回访清单

### Mahjong Copilot 日麻学习资料目录

- 地址：[立直麻将学习资料](https://mjcopilot.com/blog/tutorial)
- 用途：作为 2.0 至 4.0 课程规划、规则查漏和参考书目索引；后续提取知识点时逐条登记作者、原始来源、适用版本和授权状态。
- 版权边界：页面汇总了大量外部专栏、译著和带图资料，不能默认获得图片、扫描页或译文的再发布权。优先提取知识结构并重新表述，牌例和示意图使用本项目牌面重新制作；需要直接使用的素材必须另行确认授权。
- 相关开源项目：[MahjongCopilot](https://github.com/latorc/MahjongCopilot)。其 Mortal/Akagi 集成可在 4.0 阶段作为离线分析流水线参考；若复制或组合 GPL-3.0 代码，需单独评估项目的许可证安排。

## 20. 2.0 实施状态（2026-08-20）

2.0 已形成首个可试玩版本：

- 参考 Riichi-Trainer 的训练行为，以本项目代码独立实现逐巡“切牌、评价当前受入、自动摸牌、直到听牌”的状态机；不复制其 GPL-3.0 源代码。
- 普通手、七对子和国士无双始终在同一连续训练中参与向听、受入及最佳切牌计算，不预先锁定路线；起手可以偏向特殊手，但后续每巡仍按实际进张动态选择。
- 连续训练改为浏览器端即时随机：从含三枚赤五的完整 136 张牌中一次洗出宝牌指示物、13 张起手与其余牌山，不按向听或路线筛选，因此每局起手与整条进张都真正随机；会话 seed 写入内存以便问题复现。原 16 个固定会话仅保留为确定性回归夹具，不再作为玩家抽题池。
- 切牌模式采用单击即切并自动摸牌。每巡保存当时手牌、玩家选择、全部候选、最优选择、受入、好型绝对枚数和是否命中最优；进入听牌且存在和了牌时结束。页面百分比统一为“最优选择率 = 最优巡数 / 已答巡数”，原始受入总和不再冒充综合效率。好型严格来自 `23～78` 两面搭子的两侧进张或新形成两面的改良；对子、双碰、嵌张、边张和字牌成对均不得冒充。舍牌振听按完整理论待牌集合判断：自己牌河（含本巡候选切牌）命中任一待牌，整组待牌均按实际剩余枚数的 1/4 计入有效受入，次巡模拟也重新判断；当前没有“放过荣和”事件，暂不模拟同巡振听。
- 进张、改良和好型由同一份 34 种摸牌转移表生成。每门数牌穷举面子、对子、两面、嵌张和边张的非支配分割前沿；降向听进张与同向听结构改良分开，摸回刚切牌只按恢复原状处理，不获得改良奖励。普通手向听也拆为万、饼、索、字四门状态并按计数签名缓存。2.0 排序已移除“受入差 4 枚／次巡期望差 0.5 枚”的硬阈值：最低向听为硬前置，随后用统一两巡窗口的累计向听推进值评价直接受入与改良；越早推进贡献越大，一向听的真实两面还能通过第二巡成和机会自然增值。次巡期望继续保留为解释指标，不再直接决定答案；推进值相同时再比较好型绝对枚数、当前受入、宝牌和路线价值。
- 答错时自动展开切牌比较；速度与好型互不支配的前沿端点以绿色“速度解／好型解”接受，并自动显示另一端的等价取舍。答案摘要不再剧透随后摸牌，改为显示切后向听、受入、次巡期望、两巡推进值、好型绝对枚数及具体好型进张；展开比较也直接显示好型进张，点击好型枚数后再拆分完整明细。命中明确的搭子价值定式时，在结论右侧补充短牌理提示。
- 连续训练支持暗杠判断。暗杠后从确定性牌山翻出追加宝牌指示物，再损耗一张牌作为岭上摸牌，后续向听、受入、宝牌和可见牌均按副露面子数及多个指示物重新计算。
- 整理模式已改为整门牌分割引擎：每次进张只枚举并排序一次该门所有合理完整分割，点击只循环预计算结果；杠、面子、对子、两张搭子、连续嵌张复合搭子与浮牌均在同一模型内。浮牌永不锁定，所有候选共有的组显示绿色连续括号，其余可切换组显示金色连续括号；牌张由统一网格保证等宽。自由拖动一旦离开牌区立即结束，完成手动重排后清除旧分组标记，避免粘牌或说明错位。每次进张后仍等待玩家下一次点击才显示新快照。
- 首页“基础课程”按钮先进入独立课程目录，不再直接跳到某一道题；目录顶部提供打乱全部 84 题的“随机基础练习”，下方完整列出 14 个基础原则及各课进度。连续训练不再单独显示普通手、七对子和国士路线标签，三种路线仍由评价器动态比较。未来 3.0 内容直接扩展连续训练。
- 分组与评价回归覆盖 `2222446s`、`245568p → [2p][45p][56p][8p]`、`335577p` 双 `357`、`799m → 8m`、`1366m → 8m`，以及 `4677s` 在切不同万子后始终保留 `5s/7s/8s` 的跨花色不变量；普通手分门向听另与旧整手递归进行确定性随机差分验证。
- 仍需通过真人试玩校准连续训练的起手分布、专门路线出现频率、反馈信息密度和结束节奏；这些属于 2.0 内容与体验校准，不改变当前“向听优先、两巡累计推进值比较纯速度、好型绝对枚数说明进听质量”的评价定义。
